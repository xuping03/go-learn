# 14. 指针

## 基本用法

```go
var a int = 10
var p *int = &a  // p 保存 a 的地址
fmt.Println(*p)  // 解引用，得到 a 的值
*p = 666         // 通过指针修改 a 的值
```

## new 分配内存

```go
cc := new(int)  // 分配 int 内存，返回指针
*cc = 999
```

## 关键点

- `&` 取地址，`*` 解引用
- 指针默认零值为 `nil`
- `new(T)` 分配类型 T 的零值内存，返回 `*T`
