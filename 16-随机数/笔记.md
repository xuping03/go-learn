# 16. 随机数

```go
import (
    "math/rand"
    "time"
)

rand.Seed(time.Now().UnixNano())  // 设置随机种子（只需一次）
rand.Int()      // 很大的随机整数
rand.Intn(100)  // [0, 100) 范围内的随机整数
```

## 关键点

- 种子相同 → 每次运行产生的随机序列相同
- 推荐用 `time.Now().UnixNano()` 作为种子，保证每次不同
- Go 1.20+ 不再需要手动设置种子，默认全局随机
