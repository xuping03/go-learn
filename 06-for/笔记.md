# 06. for 循环

## 普通 for

```go
for i := 0; i < len(str); i++ {
    fmt.Println(str[i])
}
```

## range 遍历

```go
for i, data := range str {
    fmt.Println(i, data)  // i 是索引，data 是值（rune 类型）
}
```

- 遍历字符串时 `data` 是 `rune`（Unicode 码点），支持中文
- 遍历数组/切片时 `data` 是元素值
