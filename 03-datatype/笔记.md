# 03. 数据类型

## 基础类型示例

```go
var ch byte = 'a'         // 字节（字符）类型
var str = "hhhh"          // 字符串，len(str) 获取长度
var t complex128 = 2.1 + 1.28i  // 复数
real(t)  // 实部
imag(t)  // 虚部
```

## 自定义类型

```go
type bigint int64  // 基于 int64 定义新类型
```

- 自定义类型与原类型不能直接赋值，需显式转换
