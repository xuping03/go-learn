# 01. 变量声明与类型

## 变量声明方式

```go
var a int          // 声明，零值为 0
var a int = 10     // 声明并赋值
a := 30            // 短变量声明（自动推断类型，只能在函数内）
var a, b int       // 多变量声明
```

## 匿名变量（空白标识符）

```go
aa, _ = 30, 40  // _ 丢弃不需要的值
```

## 类型打印

```go
fmt.Printf("f type is %T\n", f)
```
