# 12. 获取命令行参数

```go
import "os"

list := os.Args       // 所有参数切片
n := len(list)        // 参数数量（包含程序名本身）
list[0]               // 程序名
list[1], list[2]...   // 用户传入的参数
```

## 示例

```
go run a.go hello world
// list[0] = 程序路径
// list[1] = "hello"
// list[2] = "world"
```
