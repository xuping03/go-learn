# 18. 数组作为参数

## 值传递 vs 引用传递

```go
// 数组是值传递，函数内修改不影响原数组
func modify(arr [5]int) { arr[0] = 100 }

// 传切片是引用传递，函数内修改影响原数据
func modify(s []int) { s[0] = 100 }

// 传指针也可修改原数组
func modify(arr *[5]int) { arr[0] = 100 }
```

## 关键点

- Go 中数组是**值类型**，传参时完整拷贝
- 切片、map、channel 是**引用类型**，传参时共享底层数据
