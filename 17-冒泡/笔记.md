# 17. 冒泡排序

## 核心思路

相邻元素两两比较，大的往后冒，每轮确定一个最大值到末尾。

```go
for i := 0; i < len(arr)-1; i++ {
    for j := 0; j < len(arr)-1-i; j++ {
        if arr[j] > arr[j+1] {
            arr[j], arr[j+1] = arr[j+1], arr[j]
        }
    }
}
```

- 时间复杂度：O(n²)
- Go 交换两个变量：`a, b = b, a`（无需临时变量）
