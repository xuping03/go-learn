# 20. copy

## 用法

```go
a := []int{1, 2}
b := []int{6, 6, 6, 6, 6}
copy(b, a)
fmt.Println(b) // [1 2 6 6 6]
```

## 关键点

- `copy(dst, src)`：将 src 复制到 dst
- 复制的元素数量为 `min(len(dst), len(src))`
- copy 后两个切片独立，修改互不影响
