# 09. 回调函数

## 函数类型定义

```go
type FuncType func(int, int) int
```

## 将函数作为参数传入

```go
func Calc(a, b int, fTest FuncType) (result int) {
    result = fTest(a, b)
    return
}

func Add(a, b int) int { return a + b }

a := Calc(1, 1, Add)  // 将 Add 作为回调函数传入
```

- 函数是一等公民，可以作为参数、返回值、变量赋值
